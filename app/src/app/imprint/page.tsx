import { LegalDocument } from "@/components/legal-document";
export const metadata = { title: "Imprint | Sprechen", robots: { index: false, follow: true } };
export default function ImprintPage() { return <LegalDocument page="imprint" />; }