import { LegalDocument } from "@/components/legal-document";
export const metadata = { title: "Security and beta status | Sprechen", robots: { index: false, follow: true } };
export default function SecurityPage() { return <LegalDocument page="security" />; }