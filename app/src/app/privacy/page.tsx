import { LegalDocument } from "@/components/legal-document";
export const metadata = { title: "Privacy policy | Sprechen", robots: { index: false, follow: true } };
export default function PrivacyPage() { return <LegalDocument page="privacy" />; }