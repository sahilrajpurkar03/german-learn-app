import { LegalDocument } from "@/components/legal-document";
export const metadata = { title: "Data sharing policy | Sprechen", robots: { index: false, follow: true } };
export default function DataSharingPage() { return <LegalDocument page="data-sharing" />; }